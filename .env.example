# This file is a pointer — each surface has its own .env.example:
#
#   backend/.env.example   → FastAPI + ML inference (Supabase, Postgres, Gemini)
#   mobile/.env.example    → Expo app (backend URL, Supabase anon key)
#   web/.env.example       → Vite dashboard (Supabase, optional backend URL)
#
# Copy the one you need to ".env" in the same folder and fill in the blanks.
