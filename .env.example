# MongoDB Atlas (replace <db_password> with your actual password)
MONGODB_URI=mongodb+srv://apbuenaobra6117qc_db_user:<db_password>@cluster0.0wvnqzj.mongodb.net/rapid?retryWrites=true&w=majority&appName=Cluster0

# Node API (optional - defaults to 3000)
API_PORT=3000

# Gemini API — https://aistudio.google.com/apikey (optional; template fallback if unset)
GEMINI_API_KEY=

# Optional: model id (default gemini-2.5-flash). If you see 429 quota errors on a model, switch here.
# GEMINI_MODEL=gemini-2.5-flash
