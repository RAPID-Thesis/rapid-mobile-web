# Backend API URL — used for /api/assessments/sync and /api/ai/predict/*
# Use your machine's LAN IP (not localhost) so Expo Go on a real phone can reach it.
# Find it with `ipconfig` (Windows) or `ifconfig` (macOS/Linux).
EXPO_PUBLIC_API_URL=http://192.168.1.15:8000

# Supabase — find these in Supabase Dashboard > Settings > API
EXPO_PUBLIC_SUPABASE_URL=https://zhyrmtowdomdgyecstcz.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InpoeXJtdG93ZG9tZGd5ZWNzdGN6Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzMzMjI2ODMsImV4cCI6MjA4ODg5ODY4M30.kvl-uGp-C0k_E1sAFlE6q6Nu058NGD21UQ61scvGp14

# Password reset — Supabase Redirect URLs must include this (matches app.json "scheme": rapid).
# Override only for Expo Go / dev if needed (paste the URL from Linking.createURL('/reset-password')).
# EXPO_PUBLIC_PASSWORD_RESET_REDIRECT_URL=rapid://reset-password
