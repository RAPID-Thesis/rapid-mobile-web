# RAPID backend — Python 3.11 or 3.12 (64-bit).
#
# The API does NOT run inference. Classification happens on the phone with the
# models bundled in mobile/assets/models/, and the server stores that verdict.
# TensorFlow, scikit-learn, pandas and numpy are therefore intentionally absent:
# they took the container from ~250 MB to ~2.5 GB and cost a 30-120 s cold start
# for work that no longer happens here.
#
# Retraining and the reference server-side pipeline still live in ml/ and
# backend/app/services/ml_fusion_engine.py — install ml/requirements.txt for those.

fastapi
passlib
psycopg[binary]
python-dotenv
python-multipart
sqlalchemy
supabase
uvicorn

# Action planner (no ML stack — talks to the Gemini API over HTTP)
google-genai>=0.3
