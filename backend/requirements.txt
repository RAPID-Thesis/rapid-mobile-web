# RAPID backend — use Python 3.11 or 3.12 (64-bit).
# Python 3.13+ / 3.14 on Windows: numpy & tensorflow often have NO prebuilt wheels yet,
# so pip downloads source and tries to compile → fails without MSVC ("Unknown compiler").
# Fix: install Python 3.12 from python.org, then:
#   py -3.12 -m venv .venv
#   .\.venv\Scripts\activate
#   pip install -r backend/requirements.txt

fastapi
passlib
psycopg[binary]
python-dotenv
python-multipart
sqlalchemy
supabase
uvicorn

# ML inference
joblib>=1.3
numpy>=1.26,<2.2
pandas>=2.1
scikit-learn==1.6.1
tensorflow>=2.20,<2.21
pillow>=10.0

# Action planner
google-genai>=0.3
